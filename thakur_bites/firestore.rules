rules_version = '2';

// TEMPORARY: Permissive rules for Phase 1 smoke testing
// Will be replaced with production rules when Auth is wired in Phase 7
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
